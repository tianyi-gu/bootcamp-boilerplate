{
  "scripts": {
    "dev": "set NODE_ENV=development && yarn server && yarn client",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "server": "node backend/server.js",
    "client": "cd frontend && yarn dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "mongodb": "^6.17.0"
  }
}
